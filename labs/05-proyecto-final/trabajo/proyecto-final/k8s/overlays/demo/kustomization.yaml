apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: replicas-api.yaml
  - path: cron-lento.yaml

labels:
  - includeSelectors: true
    pairs:
      env: demo